# マウス操作を有効にする
set -g mouse on

# クリップボード共有を有効にする
#set-option -g default-command "reattach-to-user-namespase -l zsh"



# バッテリー残量表示版ステータスバー (/usr/local/bin/ に755でbatteryを置いておくこと)
set-option -g status-right '#(battery --tmux) #[fg=colour255,bg=colour246] %Y/%m/%d %H:%M:%S '

